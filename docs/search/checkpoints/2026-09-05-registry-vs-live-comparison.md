# FIELD_REGISTRY vs LIVE COTALITY — FIRST CHECKPOINT COMPARISON (2026-09-05)

**Method.** Live truth was established first by the Contract/Data Validator, from the Cotality API alone, with no visibility of the registry, of #618 or of any document (report: `docs/search/evidence/2026-09-05-live-cotality-checkpoint-contract.md`, ~1,050 calls, raw stdout saved per call). Only then was `lib/search/canonical/field-registry.ts` on `main @ 2a83952a` read and compared entry by entry. Where they disagree, the live provider wins.

Universes, live: **SALE** = `StandardStatus eq 'Active' and PropertyType eq 'Residential'` → 6,638. **RENTAL** = `StandardStatus eq 'Active' and PropertyType eq 'ResidentialLease'` → 918. Under `Active`, every other PropertyType member is 0; only 4 of 11 StandardStatus members are populated anywhere (Active 7,555 · Closed 578,373 · Pending 5,597 · ComingSoon 6).

Classification vocabulary: VERIFIED_CURRENT · CORRECT_MAPPING_REQUIRED · NEEDS_LIVE_SEMANTIC_PROBE · NOT_POPULATED · NOT_AUTHORIZED · REMOVE_FROM_EXECUTION.

## Checkpoint criteria

| Mallan criterion | Registry entry today | Live Cotality truth (query in evidence file) | Classification | Correction required |
|---|---|---|---|---|
| **Status** | `standard_status` → `StandardStatus`, mapped, filterable | Picklist 11 members verbatim; populated: Active, Closed, Pending, ComingSoon; `ActiveUnderContract`, `Canceled`, `Delete`, `Expired`, `Hold`, `Incomplete`, `Withdrawn` = 0. `eq` on all 11 SUPPORTED; orderby SUPPORTED. | **VERIFIED_CURRENT** | Executor note: the default status set on `main` includes `ActiveUnderContract`, which is 0 live; harmless but must not be described as a populated state. |
| **Sale / Rental universe** | `transaction_type` → `PropertyType`, mapped; notes "sale=Residential, rental=ResidentialLease" | Under Active only `Residential` (6,638) and `ResidentialLease` (918) exist; 11 other members 0. `eq` on all 13 SUPPORTED. | **VERIFIED_CURRENT** (registry) / **CORRECT_MAPPING_REQUIRED** (executor) | Executor on `main` defines sale negatively as `PropertyType ne 'ResidentialLease'`, admitting eleven zero-count members. Sale must be positive membership `eq 'Residential'`. |
| **Price (sale)** | `list_price` → `ListPrice`, mapped, filterable, sortable | Edm.Decimal(14,2), nullable; non-null 6,638/6,638; min/max present; `ge/le`, `ne null`, `eq null`, orderby asc/desc SUPPORTED. | **VERIFIED_CURRENT** | none |
| **Rent (rental)** | no separate entry; `list_price` covers both | On RENTAL `ListPrice` non-null 918/918 (min 1,800 · max 170,000; 20-row sample 2,500–42,000). `LeaseAmount`, `TotalActualRent`, `LeaseAmountFrequency`, `ListPriceLow`, `LandLeaseAmount`, `PetDeposit` = 0 non-null. `CurrentPrice` 918, `OriginalListPrice` 916, `SecurityDeposit` 275, `MoveInCostsAmount` 23. | **VERIFIED_CURRENT** for execution on `ListPrice` · **NEEDS_LIVE_SEMANTIC_PROBE** for the period label | Execute min/max rent on `ListPrice` within the RENTAL universe. The provider gives no populated frequency field, so Mallan may not label the figure "per month" as a provider fact; the value is displayed as the provider's rent figure until a live semantic proof exists. |
| **Bedrooms** | `bedrooms` → `BedroomsTotal`, mapped | Non-null on both universes; `eq N`, `gt`, orderby SUPPORTED; distribution 0–6 recorded. | **VERIFIED_CURRENT** | none |
| **Bathrooms** | `bathrooms` → "`BathroomsFull/BathroomsHalf`", **partial**, dbColumn `bathrooms_full`; notes "diverges by engine: Full vs TotalInteger" | `BathroomsFull` and `BathroomsHalf` non-null 6,638/6,638 (SALE). `BathroomsTotalInteger` non-null 6,623 (15 null). Joint check on 200 SALE rows: Total = Full + Half in 188; Total = Full only 0; neither 12 (e.g. F=4, H=2, T=5); any null 0. `OneQuarter`/`ThreeQuarter` exist on ~30 rows, always 0; `BathroomsPartial`, `MainLevelBathrooms` unpopulated. All filterable and orderable. | **CORRECT_MAPPING_REQUIRED** | The executor on `main` filters `BathroomsTotalInteger`, which is incomplete (15 nulls), drops half baths, and disagrees with Full+Half on 12/200 rows. The bath criterion must execute on `BathroomsFull` and `BathroomsHalf` (Mallan's canonical value `Full + 0.5 × Half` is a Mallan rule over two live fields). `providerMappingStatus` becomes `mapped` with both fields named explicitly. |
| **Borough** | `borough` → **`CountyOrParish`**, partial; notes "CountyOrParish vs CityRegion split — needs_probe" | `CityRegion` carries Manhattan 4,422 / Brooklyn 1,314 / Queens 607 / Bronx 268 / **StatenIsland** 27 (sum 6,638); `CountyOrParish` carries New York / Kings / Queens / Bronx / Richmond with identical counts. Both 100% populated on both universes, `eq` and orderby SUPPORTED. `City` is the constant "New York City". `PostalCity` mixes vocabularies. | **CORRECT_MAPPING_REQUIRED** | Borough executes on `CityRegion` (borough vocabulary), with the live token `StatenIsland` (no space) mapped deterministically from Mallan's label "Staten Island". `CountyOrParish` recorded as the county equivalent, not the borough criterion. Executor on `main` already uses `CityRegion`; the registry is the one out of step. |
| **Neighborhood** | `neighborhood` → `SubdivisionName`, partial; notes "resolves 3 ways (ZIP expansion vs SubdivisionName vs name)" | `SubdivisionName` 100% populated on both universes, carries neighborhood names, `eq` and orderby SUPPORTED; **case variants exist corpus-wide** (e.g. "ASTORIA"). `MLSAreaMajor`/`MLSAreaMinor`: null in all 400 sampled rows and **PROVIDER_REJECTED** for filter and orderby. | **VERIFIED_CURRENT** (field) · **CORRECT_MAPPING_REQUIRED** (execution vocabulary) | Execute exact `eq` against the **live token vocabulary**, captured from the provider, including case variants; not against a Mallan alias file and never via `MLSArea*`. The executor on `main` expands names through `data/rls/geo/neighborhood-aliases.json`, a Mallan file that is not provider vocabulary. Vocabulary capture is the next live step (see addendum). |
| **Basic property type** | `property_sub_type` → `PropertySubType`, **type `multi_enum`**, partial; notes "structural sub-types cannot be pushed to $filter (502) → post-filtered" | `PropertySubType` is a **single**-value enum ("String List, Single" in the provider's Field resource). On SALE: `Apartment` 5,432 of 6,638; **`Condominium`, `StockCooperative`, `Townhouse` = 0** on both universes. `eq` on all 75 members SUPPORTED (no 502 observed). Condo/co-op live in `CommonInterest` (SALE: Condominium 3,282 · StockCooperative 2,350 · None 844 · Condop 127 · RentalBuilding 35; RENTAL: RentalBuilding 519 · Condominium 200 · None 128 · StockCooperative 52 · Condop 19). Townhouse lives in multi-enum `StructureType has 'Townhouse'` (518 SALE, 506 of them CommonInterest=None). `OwnershipType`, `CommonWalls` entirely null. | **CORRECT_MAPPING_REQUIRED** | (1) Registry type for `property_sub_type` becomes `enum`, not `multi_enum`. (2) Mallan's "basic property type" criterion does **not** map to `PropertySubType`. Condo / Co-op / Condop map to `CommonInterest eq` members; Townhouse maps to `StructureType has 'Townhouse'`. The `ownership` entry (`CommonInterest`) is **VERIFIED_CURRENT** and becomes the carrier. The "502 → post-filter" note describes behaviour not reproduced live and is removed from execution. |

## Supporting entries the checkpoint relies on

| Entry | Live truth | Classification | Note |
|---|---|---|---|
| `ownership` → `CommonInterest` | populated as above; `eq` on all 13 members SUPPORTED | **VERIFIED_CURRENT** | carrier for condo / co-op / condop |
| `listing_key` → `ListingKey` · `listing_id_mls` → `ListingId` | both non-null on all active rows; both filterable and orderable; `ListingKey gt '<v>'` keyset paging SUPPORTED; `ListingKeyNumeric` carries the same digits | **VERIFIED_CURRENT** (`listing_id_mls.filterable` moves from `needs_probe` to `yes`) | registry note that the public DTO emits internal `listing_id` as `mlsId` is an identity-domain defect to keep visible |
| `media` → "Media (ResourceRecordKey)", partial | `ResourceRecordKey eq <ListingKey>` and `ResourceRecordID eq <ListingId>` return the same rows; crossed forms return 0. `Order` restarts per `MediaCategory`; `PreferredPhotoYN` null on 180/200 rows. | **VERIFIED_CURRENT** with two named join keys, each in its own domain | hero selection must be category-aware; `PreferredPhotoYN` is not a dependable marker |
| `permission` → "Permission/ListingPermission" | `Permission` = `IDX` on **every** row of both universes; `has 'IDX'` / `eq 'IDX'` SUPPORTED; the runtime type is `Multi.ListingPermission`, so the namespaced `Multi.Permission` form is rejected | **VERIFIED_CURRENT** | execute with the bare-string form |
| `participant_only` → "Permission=Private" | 0 rows with `Private` in either active universe | **NOT_POPULATED** in active universes | gate stays fail-closed as a rule; it is not an execution criterion |
| `owner_opt_out` → no provider field | no live field | **NOT_AUTHORIZED** at provider | Mallan storage rule only |
| display flags `InternetEntireListingDisplayYN`, `InternetAddressDisplayYN` (gate code, no registry entry) | null in 400/400 sampled rows; `$select` accepted; `ne null`, `eq true`, `eq false`, orderby all **PROVIDER_REJECTED** 400 "suppressed (provider Level)" | **NOT_AUTHORIZED** for execution | cannot be a query axis; Mallan's fail-closed treatment is a storage/business rule over a value the provider returns as null |
| `InternetAutomatedValuationDisplayYN`, `InternetConsumerCommentYN` | populated on every row (~96.5% true), all four probes SUPPORTED | **VERIFIED_CURRENT** | per-row flags, filterable |
| `SyndicateTo` | null everywhere; every member 0 | **NOT_POPULATED** | — |
| `commercial` → `PropertyType` commercial members | 0 under Active | **NOT_POPULATED** | REMOVE_FROM_EXECUTION for this checkpoint |
| `mls_status` → `MlsStatus` | consistent with earlier live rejection | **NOT_AUTHORIZED** | unchanged |
| `list_office_*` → `ListOfficeMlsId`, `ListOfficeName` | 100% populated; `eq '<id>'` SUPPORTED | **VERIFIED_CURRENT** | carrier for return-copy suppression |

## Pagination and sort, live

- `$skip` paging: page 2 has 0 overlap and no gap against page 1 by `ListingKey`; `@odata.nextLink` present. Keyset `ListingKey gt` SUPPORTED. RENTAL full walk returned exactly 918 = `@odata.count`; SALE walk UNVERIFIED by the Validator's 5,000-row rule (6,638). Provider `$top` honoured well above 200 (earlier in session up to 20,000).
- `ListPrice` orderable both directions. A proven "newest" key is **not yet established** (see addendum).

## Multi-value syntax, live

`has '<member>'` and `eq '<member>'` work on multi-value fields with identical counts (e.g. `StructureType has 'HighRise'` 3,826). The namespaced `Multi.<Field>` form also works; the un-namespaced enum form and `/any(...)` lambdas are PROVIDER_REJECTED. `Permission` must use `has 'IDX'`, because its runtime type is `Multi.ListingPermission`.

## What live evidence contradicts

- `PropertySubType` is single-valued and never carries condo, co-op or townhouse on this feed; the registry typed it multi-valued and the executor on `main` uses `contains(PropertySubType, …)` for sub-types that are 0.
- The registry's borough field is `CountyOrParish`; the executor uses `CityRegion`; the data supports both, and the borough vocabulary is in `CityRegion`.
- The registry's bathroom entry is honest about the divergence; the executor chose the incomplete integer field.
- The "provider-gated" display flags cannot be filtered, and are null on every sampled row, so no executor may claim to filter on them.

## BUILDER EVIDENCE — NOT part of the Validator's verdict (Builder-run live probes, 2026-09-05T03:06–03:07Z)

Evidence class: **Builder**. These three probes were run by the Builder after the Validator's report, against the live Cotality API, to fill gaps in the Validator's brief. They were recorded separately and were not seen by the Validator.

**Re-check outcome (independent Contract/Data Validator, 2026-09-05, `docs/search/evidence/2026-09-05-registry-recheck-validator.md`): all 11 corrected mappings CONFIRMED, 0 contradicted, 0 unverified.** Of the three Builder probes: the sort key (item 7) and the Mallan office rows (item 8) are now Validator-confirmed; for the neighborhood vocabulary the Validator confirmed population, operators and every named case variant (item 5) but declined by rule to verify the universe-wide token count, so the vocabulary SIZE stays Builder evidence. New live nuance from the re-check: `CityRegion eq 'Staten Island'` (with a space) is accepted and returns 0, not rejected.

**Newest-first sort key.** `ListingContractDate ne null`: SALE 6,639 / RENT 918 (every row). `OnMarketDate`: 6,639 / 917. `ModificationTimestamp` and `OriginalEntryTimestamp`: every row. `probeField` on `ListingContractDate`: select, non-null (581,763 corpus), orderby and `ge` operator all SUPPORTED. **`ListingContractDate` is a fully populated, orderable "newest" key on both universes**; the registry's `listing_contract_date` entry ("canonical newest sort key") is VERIFIED_CURRENT. `ModificationTimestamp`, the executor's default on `main`, orders by last touch, not by listing date.

**Mallan's own office rows in the live universes.** `ListOfficeMlsId eq '7041'`: SALE count **2** — `RLS20099289` (ListingKey 1175519507, unit 4D, $560,000) and `RLS20093870` (1170236599, unit 2G, $765,000); RENT **0**. `ListOfficeMlsId eq '20196'`: 0 and 0. These two rows are exactly the provider return-copies of Mallan's two active local listings (SL-0007 and SL-0004). Suppression therefore removes exactly two rows from the sale universe today and none from rentals; the office criterion is the one already carried by `list_office_*`.

**Live neighborhood vocabulary.** Full walk of `SubdivisionName` (selecting `SubdivisionName, CityRegion`, ordered by `ListingKey`, complete=true): SALE 6,639 rows → **236 distinct tokens**, **7 case-variant groups** (`Midtown ~ midtown`, `Dumbo ~ DUMBO`, `Tribeca ~ TriBeCa`, `NoHo ~ Noho`, `SoHo ~ SOHO ~ Soho`, `NoMad ~ NOMAD`, `Nolita ~ NoLIta`), **0 tokens spanning more than one borough**. RENT 918 rows → 108 distinct, 1 variant group (`Dumbo ~ DUMBO`), 0 multi-borough. Captured as `docs/search/evidence/2026-09-05-subdivision-vocab-sale.json` and `-rent.json` (token → count and borough). Consequence: a neighborhood criterion executes as an `eq` over every live case variant of the chosen name, and each token resolves to exactly one borough on this feed.

## Not carried from #618 until re-verified

Nothing has been extracted from #618 yet. Its bath contract, status-token contract, property-type universe, geography contract and sort contract will each be opened only to compare against this table; any provider claim in them that this evidence does not confirm is not carried.

{
  "mcpServers": {
    "trestle-fields": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "--no-install",
        "tsx",
        "mcp/trestle-fields/index.ts"
      ],
      "env": {
        "IDX_CLIENT_ID": "${IDX_CLIENT_ID}",
        "IDX_CLIENT_SECRET": "${IDX_CLIENT_SECRET}",
        "TRESTLE_API_URL": "${TRESTLE_API_URL}"
      },
      "description": "Optional local helper for live Cotality field lookup. Live provider only; fail closed when Cotality is unavailable. This helper is not provider authority."
    }
  }
}

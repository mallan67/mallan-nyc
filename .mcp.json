{
  "mcpServers": {
    "trestle-fields": {
      "type": "stdio",
      "command": "node",
      "args": ["mcp/trestle-fields/dist/index.js"],
      "env": {
        "IDX_CLIENT_ID": "${IDX_CLIENT_ID}",
        "IDX_CLIENT_SECRET": "${IDX_CLIENT_SECRET}",
        "TRESTLE_API_URL": "${TRESTLE_API_URL}"
      },
      "description": "Live-only Cotality contract reader. Delegates to scripts/cotality/query-live.mjs for current metadata, Field/Lookup values, resource rows, pagination, field capability probes, relationships, service document, and DataSystem. No snapshot fallback; acquisition failure is UNVERIFIED."
    }
  }
}
